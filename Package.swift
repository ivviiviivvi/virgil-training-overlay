// swift-tools-version:5.5
import PackageDescription

let package = Package(
    name: "mac-tooltip",
    dependencies: [],
    targets: [
        .executableTarget(
            name: "mac-tooltip",
            dependencies: []),
    ]
)
